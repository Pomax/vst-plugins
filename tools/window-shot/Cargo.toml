[workspace]

[package]
name = "window-shot"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["markdown-notes"]
description = "Photographs one named window on macOS, and nothing else"
publish = false

[[bin]]
name = "window-shot"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 2
strip = true
