# The order the tests run in, first line first. Names, not filenames: run
# order is decided here and nowhere else. A test file that is not listed is an
# error, so nothing can sit in this directory and silently never run.
host-preset-across-runs
host-preset-dialogs
host-resize
