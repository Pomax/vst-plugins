# Bold text is drawn in the machine's bold face.
#
# It used to be the ordinary face in a stronger colour, which does not read as
# bold because it is not. The face is checked by a unit test that measures it;
# this one is here so the document is on screen to look at, and so the markers
# behave: `**` hides once the caret leaves the line.
type:This is a test, it **better** work
type:{ENTER}and a second line, to move the caret off the first
wait:500
shot:%CACHE%\bold-is-bold.png

expect:tabs is 1
expect:notes contain **better**
