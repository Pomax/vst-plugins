# The Save button, clicked for real, and the dialog it must open.
#
# The dialog is the assertion: `dialog:` looks at the screen and fails unless
# a save panel is there, so a click that lands nowhere fails here rather than
# leaving a picture for someone to look at afterwards. The picture is kept as
# well, for working out why a failure failed.
#
# The app is then force quit: the dialog is native and modal, so the window
# cannot be asked to close under it, and a killed program writes no state,
# which is why nothing here asserts on the document.
#
# This runs second, after typing: a click that does not land fails every test
# that presses a button, so a failure here stops the run.
baseline:
press:Save|501,76
dialog:Save
shot:%CACHE%\save-click-dialog.png
kill:
